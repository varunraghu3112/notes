
import React, { Component } from 'react';
import { Platform, StyleSheet, Text, View, TextInput, ScrollView, TouchableOpacity,AsyncStorage } from 'react-native';
import Main from './app/components/main'

export default class App extends Component 
{
  render()
  {
    return(
      <Main/>
    )
  }
}